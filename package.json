{
  "name": "template-vite",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"tsc --noEmit --watch\" \"vite\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^6.0.3",
    "vite": "^5.1.6"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  }
}
